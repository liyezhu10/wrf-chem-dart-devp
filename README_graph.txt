Quick start for testing:

0) Swap out MPT for OpenMPI
1) In the GPTL directory, type 'make'                -- should make this build automatically
2) Modify the GPTL location in build/mkmf.template   -- need a better way to do this too
3) In the models/cam-fv/work directory, run ./mkmf_filter
4) Run 'make'

